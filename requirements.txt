fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pdfplumber==0.11.5
python-docx==1.1.2
sqlalchemy==2.0.38
httpx==0.28.1
pydantic==2.10.6
python-dateutil==2.9.0

# Hybrid parsing — fast, deterministic, no large model downloads
# NOTE: LibreOffice (soffice) is a REQUIRED system dependency for .doc to PDF conversion.
# Install via: apt-get install -y libreoffice (Debian/Ubuntu) or download from libreoffice.org (Windows)
rapidfuzz>=3.6
flashtext>=2.7
PyMuPDF>=1.24
dateparser>=1.2
unidecode>=1.3

# NLP / NER for name extraction
spacy>=3.7

# Auth & multi-tenancy
# bcrypt 4.x removed __about__ which passlib 1.7.4 requires; pin to last compatible version
bcrypt==3.2.2
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
alembic==1.13.3
psycopg2-binary==2.9.10

# Export
pandas==2.2.3
openpyxl==3.1.5

# JD URL scraping
beautifulsoup4==4.12.3
lxml==5.3.0

# Video analysis
faster-whisper==1.1.0
yt-dlp==2025.3.31

# LangGraph multi-agent pipeline
langgraph>=0.2.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0

# Observability
prometheus-fastapi-instrumentator>=6.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.3
pytest-cov==6.0.0
respx==0.22.0
