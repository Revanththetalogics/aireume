# Speech Service Dependencies — CPU-optimized
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ML / Audio
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0
numpy>=1.24.0
pydub>=0.25.0

# TTS — Kokoro (primary, CPU-based, professional voice) + Edge TTS (fallback)
kokoro>=0.9.4
soundfile>=0.12.0
misaki[en]>=0.1.0
edge-tts>=6.1.0

# STT — faster-whisper (CPU INT8, 2-4x faster) with openai-whisper fallback
faster-whisper>=1.1.0
openai-whisper>=20231117

# Silero VAD
# Installed via torch.hub at runtime (no pip package needed)
